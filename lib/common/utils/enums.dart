enum QueryType {
  all,
  samsung,
  apple,
  huawei,
  xiaomi,
  tecno,
  infinix,
  itel,
  tablettes,
  accessoires,
}

enum WishlistToggle {
  retrieve,
  delete,
  add,
}

enum FetchOrdersTypes {
  pending,
  delivered,
  cancelled,
}

enum ReloadCount {
  delete,
  reload,
  update,
}

enum NotificationCount {
  update,
  reload,
}
